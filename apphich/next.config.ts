// Nhập kiểu NextConfig để đảm bảo an toàn kiểu
import type { NextConfig } from "next";

// Tạo và cấu hình ứng dụng Next.js
// Đối tượng này chứa tất cả các cài đặt cấu hình Next.js
const nextConfig: NextConfig = {
  /* các tùy chọn cấu hình ở đây - thêm các cài đặt tùy chỉnh như chuyển hướng, viết lại, biến môi trường, v.v. */
};

// Xuất cấu hình để Next.js sử dụng khi xây dựng và chạy ứng dụng
export default nextConfig;
