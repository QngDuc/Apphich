// Cấu hình PostCSS để xử lý CSS với các plugin
const config = {
  // Danh sách các plugin PostCSS để áp dụng cho bảng kiểu
  plugins: {
    // Plugin PostCSS của Tailwind CSS
    // Xử lý các lớp tiện ích Tailwind và tạo ra CSS được tối ưu hóa
    "@tailwindcss/postcss": {},
  },
};

// Xuất cấu hình PostCSS
export default config;
